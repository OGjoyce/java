(define (main)
(clear)
(display "
Ingrese el primer numero")
(define x (read))
(display "
Ingrese el segundo numero")
(define y (read))
 (MFC x y)
)
(main)

	(if (= y 0)
	x
		(begin
			(if (= (MFC y (remainder x y))1)
				(display "Primos")
				
				(display "no son primos")

)
)
)
)


